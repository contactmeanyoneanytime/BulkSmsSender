name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # Delete all wrapper files and any corrupted properties
      - name: Remove Gradle wrapper
        run: |
          rm -f gradlew gradlew.bat
          rm -rf gradle/wrapper
          rm -rf .gradle
          if [ -f gradle.properties ]; then
            sed -i '/-javaagent/d' gradle.properties
            sed -i '/org.gradle.internal.agent/d' gradle.properties
          fi

      # Create proper gradle.properties with AndroidX enabled
      - name: Setup gradle.properties
        run: |
          cat > gradle.properties << 'EOF'
          org.gradle.jvmargs=-Xmx2048m
          android.useAndroidX=true
          android.enableJetifier=true
          kotlin.code.style=official
          EOF

      # Manually download and install Gradle 8.7 (no wrapper, no checksum verification)
      - name: Install Gradle 8.7
        run: |
          wget https://services.gradle.org/distributions/gradle-8.7-bin.zip
          unzip -q gradle-8.7-bin.zip
          echo "$PWD/gradle-8.7/bin" >> $GITHUB_PATH

      # Override project‑level build.gradle.kts with known compatible versions (safe)
      - name: Ensure correct plugin versions
        run: |
          cat > build.gradle.kts << 'EOF'
          plugins {
              id("com.android.application") version "8.5.0" apply false
              id("org.jetbrains.kotlin.android") version "1.9.20" apply false
              id("com.google.devtools.ksp") version "1.9.20-1.0.14" apply false
          }
          EOF

      # Build the debug APK using the manually installed Gradle (not the wrapper)
      - name: Build APK
        run: gradle assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: BulkSmsSender-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
